/*SELECT VERSION();
SELECT SIN(3*PI()/4), (4+1)*5;
SELECT VERSION(), NOW(), CURRENT_DATE, CURRENT_TIME;
show DATABASES;
CREATE DATABASE db1;
SHOW TABLES;*/

CREATE TABLE Parts
(Part_ID INTEGER UNSIGNED NOT NULL,
Part_name VARCHAR(80),
Material VARCHAR(80),
Weight INTEGER CHECK (WEIGHT>0));
